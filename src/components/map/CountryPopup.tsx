import React from "react";
import { AirtableRecord, EstimateGradePrevalenceFeature } from "../../types";
import Translate from '../../utils/translate/translateService';
import { getGeography, getPossibleNullDateString } from '../../utils/utils';
import { EstimateGradePrevalence, LanguageType } from "types";

export default function CountryPopup() { //feature: EstimateGradePrevalenceFeature, language: LanguageType
    return (<React.Fragment>
        <div className="popup-title">
        </div>
    </React.Fragment>)
}